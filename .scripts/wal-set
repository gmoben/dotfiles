#!/usr/bin/env bash

(wal -i $@ -n --backend haishoku || wal -i $@ -n) && \
    tmux source-file $HOME/.tmux.conf && \
    emacsclient -e "(load-theme 'ewal-spacemacs-classic)"

source $HOME/.cache/wal/set_nitrogen.sh
